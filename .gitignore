node_modules/
dist/
.vite/
.ssr/
.env*
# ...but the documentation template must be tracked. It contains variable
# names and comments only, never values.
!.env.example
.DS_Store

# OS / editor artifacts
.idea/
.vscode/
*.log

# FUSE artefacts from mounted filesystem
.fuse_hidden*

# build stamp — generated by scripts/write-build-info.mjs at build time,
# served at /build-info.json, read by infrastructure/scripts/check_deploys.py.
# Generated, never committed: a committed copy would go stale and lie.
public/build-info.json
